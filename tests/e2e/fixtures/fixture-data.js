/**
 * The default fixtures data is shaped according to REST API
 */

/**
 * Settings fixture data, using the update batch endpoint.
 */
const Settings = () => [
	
];

module.exports = {
	Settings
};
